const port = 8000;

const whitelistMode = true; // Disabling enabling the ip whitelist mode. true/false

const whitelist = ['::1', '127.0.0.1']; // Example of adding multiple IP addresses to the whitelist: ['::1', '2001:db8::', '127.0.0.1', '192.168.0.10']

const autorun = true; // Autorun in the browser. true/false

const preferV4 = true; // prefer ipv4 for stuff like Autorun. true/false

const disableV4 = false; // disable ipv4

const disableV6 = false; // disable ipv6

const characterFormat = 'png'; // webp or png

const charaCloudServer = 'https://tavernai.net'; //'https://tavernai.net'; http://127.0.0.1


const listenIpV6 = null; // The IP address to which the socket is bound, set automatically if left as null. This is an advanced setting; do not modify the setting unless you have knowledge of networking.

const listenIp = null; // The IP address to which the socket is bound, set automatically if left as null. This is an advanced setting; do not modify the setting unless you have knowledge of networking.

const connectionTimeoutMS = 4*60*1000; // (4 mintues, change the first number if you have a really slow local API.) The maximum duration of the request, after which TAI assumes the server will not reply at all (default is 2 minutes)

const csrf_token = true;

module.exports = {
  port, whitelist, whitelistMode, autorun, characterFormat, charaCloudServer, listenIpV6, listenIp, disableV4, disableV6, preferV4, connectionTimeoutMS, csrf_token
};
